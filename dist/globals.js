var f = {
}
var g = {
    /*cart: [],
    addToCart: (e) => {
        console.log('g.addToCart running')
        let id = e.target.dataset.id;

        let item = {}
        item.id = id
        let event = events.find( event => event.id === id )
        item.name = event.title
        item.price = event.register.fee

        g.cart.concat(item)
        console.log(JSON.stringify(g.cart))
    }*/
}